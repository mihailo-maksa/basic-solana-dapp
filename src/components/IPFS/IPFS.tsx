import React from 'react'

// Create an interface similar to the nft.storage site
// Upload, pin, retrieve, delete files + login with MetaMask

// Upload - done by you
// Pin - done automatically by web3.storage
// Retrieve - done by you
// Deleted & edit - cannot be done
// Bonus: Login with MetaMask before uploading files, signature saved to localStorage and asked to verify it's you before retrieving files

const IPFS = () => {
  return (
    <div className="IPFS">
      <h1>IPFS Crash Course</h1>
    </div>
  )
}

export default IPFS
